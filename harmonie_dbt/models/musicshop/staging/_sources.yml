# musicshop/staging/_sources.yml
version: 2

sources:
  - name: raw_musicshop
    database: "{{ env_var('DBNAME_MUSICSHOP') }}"
    schema: raw
    tables:
      - name: raw_orders
        description: "Commandes brutes générées à partir de hbm.public.TB_partition et de clients fictifs"
      - name: exchange_rates
        description: "Taux de change quotidiens"
      - name: customer
        description: "Clients fictifs générés par Faker"

